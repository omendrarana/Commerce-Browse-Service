using GraphQL.Types;

namespace Commerce.Browse.Service.Infrastructure.GraphQL
{
    public class GraphSchema: Schema
    {
        //public GraphSchema(IDependencyResolver resolver) : base(resolver)
        //{
        //    Query = resolver.Resolve<>(productModel);
        //}

    }
}
